---
// Previous/next page links are disabled site-wide.
//
// Starlight has no global config flag for pagination — it is controlled
// per-page via the `prev`/`next` frontmatter. Overriding its Pagination slot
// with a component that renders nothing turns it off everywhere at once.
//
// To restore the theme's original implementation:
//   git checkout 25e0c10 -- src/components/override-components/Pagination.astro
---
